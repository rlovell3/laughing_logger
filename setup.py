from setuptools import setup, find_packages

setup(
    name='laughing_logger',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        
    ],
    # Author Ross Lovell (HA,HA, HA, HA, ...)
)
